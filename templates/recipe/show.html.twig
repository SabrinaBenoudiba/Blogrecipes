{% extends 'base.html.twig' %}

{% block title %}Recette{% endblock %}

{% block body %}

<div class="card" style="width: 18rem;">
    <img src="{{ recipe.image }}" class="card-img-top" alt="...">
    <div class="card-body">
        <h5 class="card-title">{{recipe.title}}</h5>
        <p class="card-text">{{recipe.description}}</p>
            <div>
                {% for comment in comments %}
                    <div>{{comment.user.name}} a dit : </div>
                    <div>{{comment.content}}</div>
                {% endfor %}
                </div >
        <a href="{{ path('app_comment_new', {'id': recipe.id})}}" class="btn btn-primary">Ajouter un commentaire</a>
    
        <a href="{{ path('app_recipe_index') }}" class="btn btn-primary">back to list</a>

        <a href="{{ path('app_recipe_edit', {'id': recipe.id}) }}" class="btn btn-primary">edit</a>
    </div>
</div>

    {{ include('recipe/_delete_form.html.twig') }}
{% endblock %}
