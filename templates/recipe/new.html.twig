{% extends 'base.html.twig' %}

{% block title %}Nouvelle recette{% endblock %}

{% block body %}
    <h1 style="text-align:center">Créer une nouvelle recette</h1>

    {{ include('recipe/_form.html.twig') }}

    <div class="text-center mt-3">
        <a href="{{ path('app_recipe_index') }}">Retour à la liste</a>
    </div>
{% endblock %}
